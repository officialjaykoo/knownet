{
  "name": "KnowNet",
  "url": "https://REPLACE_WITH_TUNNEL_OR_DOMAIN/mcp",
  "authentication": "none",
  "notes": [
    "The HTTP bridge holds KNOWNET_AGENT_TOKEN in its environment.",
    "Use a short-lived scoped token for quick-tunnel tests.",
    "Revoke the token immediately after the external review.",
    "Use standard MCP resources, tools, and prompts only."
  ]
}
